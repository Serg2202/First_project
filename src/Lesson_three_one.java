import com.sun.scenario.effect.impl.sw.sse.SSEBlend_SRC_OUTPeer;

import java.util.Arrays;

/**
 * Created by USER on 17.10.2016.
 */
public class Lesson_three_one {
    public static void main(String[] args) {
/*int a=12;
        //int[] a = {1, 2, 3, 4, 5};//одномерный массив , т.е. одна строка
       int[][] a = {{1, 2, 3, 4, 5}, //двумерный массив
                {1, 2, 3, 4, 5}};
/*
        for (int[] x : a) {
            for (int i : x)
                System.out.print(i + "");

            System.out.println();


        }
      */
/*
        int sum = 0;
        int nums[][] = new int[3][5];//3 вагона и 5 мест, т.е мы инициализировали массив. (т.е. выделилась память)
        //give nums more values
        for (int i = 0; i < 3; i++)
            for (int j = 0; j < 5; j++)
                nums[i][j] = (i + 1) * (j + 1);
        //use for-each for to display and sum the values
        for (int x[] : nums) {
            for (int y : x) {
                System.out.println("Value is " + y);
                sum += y;
            }
        }
        System.out.println("Simmulation: " + sum);
        // есть метод deepToString это чисто метод для жавы для вывода двумерных массивов
        */
/*
        System.out.println(Arrays.deepToString(a));
        for (int i = 0; i < 3; i++) {
        int sumLine=0;
            for (int j=0;j<5;j++){
                System.out.println(a[i][j] + "");
                sum +=a[i][j];
                sumLine +=a[i][j];
            }
        }
       /*
for (int i=0;i<10; i++){
    if (i==5) break;
    System.out.println(i);
}
*/
String a="tgthreyh";
        System.out.println("tgetg".length());
        String s = new String("hh");// объект строка это всегда объект
        System.out.println(new String("rtgrtghteh").equals(s));// сравнить 2 строки

    }

}

